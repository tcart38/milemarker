PORT=3002
DATA_DIR=./.data
NODE_ENV=development
# Hostnames allowed in the Host header besides localhost/*.local/IP literals
# (DNS-rebinding protection). Comma-separated, e.g.: milemarker.home.lan,mybox
#ALLOWED_HOSTS=
